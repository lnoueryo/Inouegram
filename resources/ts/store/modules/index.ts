
const state = {

};
const mutations = {
    profileInfo(state: any, ) {

    }
};
const actions = {
    profileInfo (context: any, ) {
        context.commit('profileInfo', );
    }
};
export default {
    state,
    mutations,
    actions,
}
